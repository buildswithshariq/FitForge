import { LoginForm } from "@/components/auth/"

export const metadata = {
  title: "Login",
  description: "Login to your FitForge account",
}

export default function LoginPage() {
  return <LoginForm />
}
