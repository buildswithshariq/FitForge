import { SignupForm } from "@/components/auth/signup-form"

export const metadata = {
  title: "Sign Up",
  description: "Create a new FitForge account",
}

export default function SignupPage() {
  return <SignupForm />
}
